---
title: "[C] 1978번: 소수 찾기"
published: 2026-03-07 20:50:00
description: "백준 1978번 소수 찾기 문제를 C로 풀이합니다."
image: "./baekjoon.png"
tags: [C, BOJ]
category: "BOJ"
draft: false
---

## 문제

<https://www.acmicpc.net/problem/1978>

주어진 수 N개 중에서 소수가 몇 개인지 찾아서 출력하는 프로그램을 작성하시오.

## 입력

첫 줄에 수의 개수 N이 주어진다. N은 100이하이다. 다음으로 N개의 수가 주어지는데 수는 1,000 이하의 자연수이다.

## 출력

주어진 수들 중 소수의 개수를 출력한다.

## 풀이

입력받은 수들을 하나씩 확인하면서 소수인지 판별하면 됩니다.

소수는 1보다 크고, 1과 자기 자신만을 약수로 가지는 수입니다.  
따라서 어떤 수 `num`에 대해 `2`부터 `num - 1`까지 나누어 보면서 나누어떨어지는 값이 하나라도 있으면 소수가 아닙니다.

- `1`은 소수가 아님
- 소수이면 개수를 1 증가
- 모든 수를 확인한 뒤 소수의 개수를 출력

## 코드

```c
#include <stdio.h>

int is_prime(int num) {
    if (num == 1) {
        return 0;
    }

    for (int i = 2; i < num; i++) {
        if (num % i == 0) {
            return 0;
        }
    }

    return 1;
}

int main() {
    int n;
    int count = 0;

    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        int num;
        scanf("%d", &num);

        if (is_prime(num)) {
            count++;
        }
    }

    printf("%d\n", count);
    return 0;
}
```
