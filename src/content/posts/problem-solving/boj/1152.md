---
title: "[C] 1152번: 단어의 개수"
published: 2026-03-07 19:30:00
description: '백준 1152번 단어의 개수 문제를 C로 풀이합니다.'
image: './baekjoon.png'
tags: [C, BOJ]
category: 'BOJ'
draft: false 
---

## 문제

<https://www.acmicpc.net/problem/1152>

영어 대소문자와 공백으로 이루어진 문자열이 주어진다. 이 문자열에는 몇 개의 단어가 있을까? 이를 구하는 프로그램을 작성하시오. 단, 한 단어가 여러 번 등장하면 등장한 횟수만큼 모두 세어야 한다.

## 입력

첫 줄에 영어 대소문자와 공백으로 이루어진 문자열이 주어진다. 이 문자열의 길이는 1,000,000을 넘지 않는다. 단어는 공백 한 개로 구분되며, 공백이 연속해서 나오는 경우는 없다. 또한 문자열은 공백으로 시작하거나 끝날 수 있다.

## 출력

첫째 줄에 단어의 개수를 출력한다.

## 풀이

문자열을 처음부터 끝까지 확인하면서, **단어가 시작되는 위치**를 세면 됩니다.

현재 문자가 공백이 아니고, 문자열의 처음이거나 바로 앞 문자가 공백이면 새로운 단어가 시작된 것입니다.

## 코드

```c
#include <stdio.h>

int main() {
    static char str[1000002];
    int count = 0;

    fgets(str, sizeof(str), stdin);

    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] != ' ' && str[i] != '\n' &&
            (i == 0 || str[i - 1] == ' ')) {
            count++;
        }
    }

    printf("%d\n", count);
    return 0;
}
```
